{
  "name": "@heythien/bible-book-map",
  "version": "1.0.3",
  "module": "index.ts",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build src/index.ts --outdir=dist --target=node && bun run build:types",
    "build:types": "bunx tsc --emitDeclarationOnly --project tsconfig.types.json",
    "typecheck": "bunx tsc --noEmit",
    "test": "bun test --coverage"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
